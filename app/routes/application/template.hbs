<Utilities::SkipLink />

{{#if (or (media 'isSmall') (media 'isMedium'))}}
  {{#if (isRootUrl)}}
    {{outlet}}
  {{else}}
    <Dynamic::Ui::Navigation::Mobile>
      {{outlet}}
    </Dynamic::Ui::Navigation::Mobile>
  {{/if}}
{{else}}
  {{outlet}}
{{/if}}
